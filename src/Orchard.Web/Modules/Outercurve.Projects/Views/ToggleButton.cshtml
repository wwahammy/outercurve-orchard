@{

    // prevent the shape from being rendered twice in a page
    if (WorkContext.GetState<object>("ToggleButton") != null) {
        return;
    }

    WorkContext.SetState("ToggleButton", new object());

   // Style.Require("jQueryUI_DatePicker");
    Script.Require("jQueryUI_Button");
}